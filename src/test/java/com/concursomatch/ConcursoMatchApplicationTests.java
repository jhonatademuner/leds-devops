package com.concursomatch;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ConcursoMatchApplicationTests {

	@Test
	void contextLoads() {
		// This test ensures that the Spring application context loads successfully.
		// It is left empty intentionally, as the presence of the @SpringBootTest annotation
		// is enough to validate that the application starts without issues.
	}

}
